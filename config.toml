[parser]
# Символы, по которым режем строку
separators = [",", ";", "/", "&"]
# Слова, которые считаем за разделители (пока выкидываем, как ты и сказал)
feat_keywords = ["feat.", "ft.", "feat", "ft", "with"]
exceptions = ["MiyaGi & Эндшпиль", "AC/DC"]
year_length = 4

[ui]
max_comment_length = 60