[package]
name = "rap"
version = "0.3.7"
edition = "2024"

[dependencies]
rap-engine = { path = "rap-engine" }
tokio = { version = "1.50.0", features = ["macros", "rt-multi-thread", "time"] }


[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
