[package]
name = "rap"
version = "0.3.6"
edition = "2024"

[dependencies]
rap-engine = { version = "0.1.0", path = "rap-engine" }
tokio = { version = "1.50.0", features = ["full"] }


[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
